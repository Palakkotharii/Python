print("Welcome to the tuples in the python")

# Mutable=> can change  
# Immutable=> cannot  change  
# eg lists are mutable , tuples are immutable

tp=(1,2,3)
# tp[1]=9 # here this will show an error becoz tuple ki value doesn't change 

print(tp)
tp1=(1,) # here if we declare only 1 tuple value then it will not consider as the tuple that's why we have to give the commma after that !!
print(tp1)

